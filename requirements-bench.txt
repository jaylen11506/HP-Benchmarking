# Python packages for the benchmark harness.
# Installed by BOTH docker/bench.Dockerfile and the Colab setup cell,
# so every machine runs identical Python code.
# Exact versions (==) so results can't shift because a package updated.
# Versions checked against PyPI on 2026-09-24.

psutil==7.2.2            # system RAM/CPU readings -> peak_sys_mem_gb
nvidia-ml-py==13.610.43  # NVIDIA GPU readings: memory, temperature, power telemetry (you import it as `pynvml`)
huggingface_hub==2.0.0   # downloading model files at an exact revision
requests==2.34.2         # sending single requests to llama-server
aiohttp==3.14.3          # sending many requests at once for B4 concurrency
lm_eval[api]==0.4.13     # B7 accuracy tests (MMLU, GSM8K) run against llama-server
jsonschema==4.26.0       # checking every result row against the schema
